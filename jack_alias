
# Remap Keyboard
# xmodmap ~/.Xmodmap

# set keyboard repeat rate manually
# (Note that on osx using KeyRemap4MacBook 240/30 feels the same as 240/50 through xset)
# Keyboard tweaks including repeat rate and caps lock to escape are set in "on_resume.sh"
source ~/.utils/on_resume.sh

# Manually tell Xfce to use capslock as escape
alias caps='/usr/bin/setxkbmap -option "caps:escape"'
# Allow a manual override to completely null out the capslock (Useful while using Jira)
alias nocaps='/usr/bin/setxkbmap -option "caps:none"'

alias start_postgres='/usr/lib/postgresql/9.1/bin/postgres -D /var/lib/postgresql/9.1/main -c config_file=/etc/postgresql/9.1/main/postgresql.conf &
'
alias be='bundle exec'
alias bs='git branch && git status'
alias h='cd ~/h'
alias alt1='TEST_ENV_NUMBER=1'
alias alt2='TEST_ENV_NUMBER=2'
alias rc='bundle exec rails console'
#alias rs='USING_SERVER=true bundle exec rails server -b 0.0.0.0'

# Note when using the "thin" invocation, you must tail the logs if you want to see them
# (they don't automatically show up in the console where you started thin)
alias rss='bundle exec thin start --ssl --ssl-key-file ~/.ssh/elitecare/server.key --ssl-cert-file ~/.ssh/elitecare/server.crt -a 0.0.0.0 -p 3000'
alias rssp='bundle exec thin start --ssl --ssl-key-file ~/.ssh/elitecare/server.key --ssl-cert-file ~/.ssh/elitecare/server.crt -a 0.0.0.0 -p 3456'

# Run locally in production mode
# Note must run over ssl
# Note assets will be served locally
alias rssproduction="RAILS_SERVE_STATIC_FILES=1 RAILS_ENV=production rss"

# alias rsi='echo "to enable logging, use rails server instead of thin start";ALLOW_NON_SSL=1 bundle exec thin start -a 0.0.0.0 -p 3000'
alias rsi='ALLOW_NON_SSL=1 bundle exec rails server -b 0.0.0.0 -p 5000'

alias db='cd ~/r/elitecarerails && bundle exec rails dbconsole -p'

alias uni='PORT=5000 bundle exec unicorn -c config/unicorns/default.rb -p 5000'
alias sin='bundle exec rerun -b "ruby app.rb"'
#alias cl='cd ~/r/sinatra_scraper'
alias cl='cd ~/cl'
alias wi='cd ~/r/wiki/content'
alias mig='RAILS_ENV=test bundle exec rake db:migrate && bundle exec rake db:migrate'
alias redo='RAILS_ENV=test bundle exec rake db:migrate:redo && bundle exec rake db:migrate:redo'
alias mus='mpg123 ~/Music/03\ Fort\ Knox.mp3'
alias p='cd ~/r/poetry'
alias da='cd ~/r/dancer'
alias sho='cd ~/r/shoelaces'
alias he='cd ~/r/healing'



# Git
alias gu='git fetch origin && git rebase origin/master'
alias log='git log --name-status'
alias keep="git add . && git commit -am'placeholder'"
alias keeps='keep; git push origin master'
alias keeph='keep; git push origin HEAD'
alias tags='git show-ref --tags'
alias housecleaning='git branch --merge | grep -v master | xargs git branch -d'

# Sass
alias sassw='cd ~/r/freecinc && bundle exec sass --watch ~/r/freecinc/public/sass:/vagrant/r/freecinc/public'

# freecinc_rackup
alias ru_freecinc='bundle exec rackup config-freecinc.ru -p 9952'

# rerun
alias re='cd ~/r/freecinc && bundle exec rerun freecinc.rb -i *.css -i *.sass --background'

# Rackspace
alias rackdev='ssh -A dev@209.20.74.96'
rack='dev@209.20.74.96'

# Digital Ocean
DO_IP='162.243.156.90'
dodaily_lager="dev@$DO_IP"
alias dodaily_lager="ssh -A $dodaily_lager"


alias docass='ssh -A dev@192.241.224.60'

# FreeCinc
dofreecinc='107.170.212.250'
alias dofreecinc="ssh -A dev@$dofreecinc"
dofreecinc_staging='dev@107.170.222.159'
alias dofreecinc_staging="ssh -A $dofreecinc_staging"

alias grab_db="scp -i ~/.ssh/id_rsa $dofreecinc_staging:dancer/db/production.db ~/r/dancer/db/development.db"
alias export_db="sudo -u postgres pg_dump -f /var/lib/postgresql/backups/bip-development-`date +'%Y-%m-%dT%H:%M:%S'`.sql bip_development"
alias import_db="sudo -u postgres psql -d bip_development  -f"

alias fr='cd ~/gopath/src/freecinc'
alias mg='cd ~/gopath/src/github.com/jackdesert/memgo'

# Conversation Timer
alias rcc='cd ~/r/relationship_chess_clock'

# Private IP Addresses



# MAC Addresses
nickel_wifi_mac='00:16:EB:11:78:06'

# Local SSH Aliases
alias nickel='ssh -A jd@$nickel'

# Remote SSH Aliases
alias rnickel='ssh -A jd@$router -p 24'

# Remote ENV VARS (for scp) (Note capital 'P')
rnickel='-P 24 jd@$router'

alias wake_nickel='sudo etherwake -i wlan0 $nickel_wifi_mac'
#alias push='gem build breadnbutter.gemspec && gem install breadnbutter-0.0.1.gem'
#alias ff='find -iname'
#alias hint='grunt jshint'
#alias turn_on_ssh_agent='eval $(ssh-agent)'
#alias remember='eval $(ssh-agent) && ssh-add ~/.ssh/id_rsa'

# Vagrant
alias vs='vagrant ssh -- -A'
alias vu='vagrant up'
alias vus='vagrant up && vagrant ssh -- -A'
alias vp='vagrant provision'
alias vf='vi portable/Vagrantfile'
alias viv='vi ~/portable/Vagrantfile'
alias vir='vi ~/portable/README.md'

alias rr='bundle exec rake routes | grep'
alias pass="git commit -am'placeholder--Its RUNNING'"

# Oft-edited Accounts files
alias acc='view ~/.secret/accounts'
alias acce='vi ~/.secret/accounts'

alias accc='view ~/.secret/accounts_cloudability'
alias accce='vi ~/.secret/accounts_cloudability'

alias accb='view ~/.secret/accounts_elite_care'
alias accbe='vi ~/.secret/accounts_elite_care'

alias acck='view ~/.secret/accounts_kids'
alias accke='vi ~/.secret/accounts_kids'

alias ali='view ~/.utils/jack_alias'
alias alie='vi ~/.utils/jack_alias'


#   Aliases using my Nav class, which goes upward until it finds a gemfile, then cds
alias m='cd $(ruby ~/.utils/nav.rb m)'
alias v='cd $(ruby ~/.utils/nav.rb v)'
alias c='cd $(ruby ~/.utils/nav.rb c)'
alias r='cd $(ruby ~/.utils/nav.rb r)'
alias j='cd $(ruby ~/.utils/nav.rb j)'
alias s='cd $(ruby ~/.utils/nav.rb s)'
alias h='cd $(ruby ~/.utils/nav.rb h)'

alias pp='ps aux | grep '
alias sb='source ~/.bashrc'
jj='jackdesert@jackdesert.webfactional.com'
jjr='jackdesert@jackdesert.webfactional.com:webapps/all_git/repos'
alias blog='cd ~/r/blog'
alias startpostgres='sudo -u postgres /usr/local/pgsql/bin/postgres -D /usr/local/pgsql/data &'

# Sup uses the system ruby, so there's a folder where rbenv is set to use system ruby locally
# This command changes to that directory before running Sup, then comes back afterward
alias sup='dir_before_sup=`pwd` && cd ~/sup && sup-mail && cd $dir_before_sup && unset dir_before_sup'


# This only applies to Crucial hdd on MacBook Pro where I built postgres from source
export PATH=/usr/local/pgsql/bin:$PATH

# Tell git to use vim
export EDITOR=vim

# Tell apt-get to do as its told
export DEBIAN_FRONTEND=noninteractive

#alias on='synclient TouchpadOff=0'
#alias off='synclient TouchpadOff=1'

# Git
alias gg='git grep -i'

# Find
alias ff='find . | grep'

# my external hdd
#alias linux='sudo mount -t ext2 /dev/sdb2 /media/jd/linux && cd /media/jd/linux'



# Mount Apple File Protocol Shares
# Example:
#   apple technology
#alias apple='ruby ~/.utils/applemount.rb'
#alias applec='gvfs-mount afp://jack@10.1.10.10/Creative/ && cd /run/user/jd/gvfs/afp-volume:host=10.1.10.10,user=jack,volume=Creative/'
#alias applet='gvfs-mount afp://jack@10.1.10.10/Technology/ && cd /run/user/jd/gvfs/afp-volume:host=10.1.10.10,user=jack,volume=Technology/'
#alias appleu='gvfs-mount -e'  # unmounts all
#alias applego='cd /run/user/jd/gvfs/afp-volume:host=10.1.10.10,user=jack,volume=Creative'
#alias applego='cd Juniper/_New\ Network\ Innovators\ \(2012\)/JUN12351\ Campus\ -\ Simply\ connected\ Tool/Photoshop/'

# Enable Palm Detection
#synclient PalmDetect=1

# Set keyboard map since Ubuntu loses it after suspend
#setxkbmap 'us(altgr-intl)'

alias publish='scp -r index.html scripts scss/css img $jj:webapps/static'

# Redshift
#alias rs1='redshift -g 0.4:0.4:0.4 -l 37:-122 &'
#alias rs2='redshift -g 0.2:0.2:0.2 -l 37:-122 &'

# Ack
alias ack='ack-grep'

# screen
alias scr='screen -D -R jack'
alias scrc='screen -D -R jack_server'
alias irc="ssh $rack 'screen -D -R irc'"

alias jl='cd ~/r/jacklisted'
alias dl='cd ~/r/daily_lager'
alias th='cd ~/r/freecinc'
alias day='cd ~/r/day'
alias gui='cd ~/cl/gui'
alias luc='cd ~/cl/lucinda'
# JAVA
#export JAVA_HOME=/usr
#export CLASSPATH='.:/home/vagrant/j/class_files/algs4_files/:/home/vagrant/j/class_files/stdlib/:/home/vagrant/j/class_files/testng/:/home/vagrant/j/class_files/jcommander/src/main/java/com/beust/jcommander:/home/vagrant/j/class_files/junit'

#TEMP
alias fstop='pkill -f unicorn ; pkill -f zookeeper ; pkill -f mailcatcher ; pkill -f ruby; sudo service mysql start'

alias gu='echo use with caution; git fetch origin && git rebase origin/master'
# Coworkers' aliases

alias ga="git add"
alias gc="git commit"
alias cbe='CARDENIO_DEVELOPMENT=true bundle exec'
alias gst='git status'
#grr(){
#  CARDENIO_DEVELOPMENT=true bundle exec $@
#}
#

# Tell zsh not to autocorrect
DISABLE_CORRECTION='true'

# Cloud
alias whats_on_staging='git branch -r --contains $(curl https://s.cloudability.com/sha -s)'
alias whats_on_prod='git branch -r --contains $(curl https://app.cloudability.com/sha -s)'
auth_token='pyCDemcw6BNPAriuPzTv'
clutil='jd@ec2-54-90-85-238.compute-1.amazonaws.com'
alias clutil='ssh -A -i ~/.ssh/skeleton01.pem jd@ec2-54-90-85-238.compute-1.amazonaws.com'

# Letters

# Amazon
mymicro='ubuntu@ec2-52-27-211-251.us-west-2.compute.amazonaws.com'
alias mymicro="ssh -A -i ~/.ssh/jacks-amazon-key.pem $mymicro"
mysmall='ec2-54-191-148-125.us-west-2.compute.amazonaws.com'
alias mysmall="ssh -A -i ~/.ssh/jacks-amazon-key.pem ubuntu@$mysmall"
mymedium='ec2-54-200-191-207.us-west-2.compute.amazonaws.com'
alias mymedium="ssh -A -i ~/.ssh/jacks-amazon-key.pem ubuntu@$mymedium"

mylarge='ec2-54-164-79-58.compute-1.amazonaws.com'
alias mylarge="ssh -A -i ~/.ssh/jacks-amazon-key-useast.pem ubuntu@$mylarge"
mylarge2='ec2-54-172-80-175.compute-1.amazonaws.com'
alias mylarge2="ssh -A -i ~/.ssh/jacks-amazon-key-useast.pem ubuntu@$mylarge2"

riak_test='ec2-54-144-131-181.compute-1.amazonaws.com'
alias riak_test="ssh -A -i ~/.ssh/night-train-keypair.pem ubuntu@$riak_test"

the_troll_ec2='ec2-54-237-55-6.compute-1.amazonaws.com'
alias the_troll_ec2="ssh -A -i ~/.ssh/dev-skeleton01.pem ubuntu@$the_troll_ec2"

bandai='ec2-54-237-118-82.compute-1.amazonaws.com'
alias bandai="ssh -A -i ~/.ssh/night-train-keypair.pem ubuntu@$bandai"

burnside='ec2-54-145-193-254.compute-1.amazonaws.com'
alias burnside="ssh -A -i ~/.ssh/dev-skeleton01.pem ubuntu@$burnside"
alias un='cd ~/gopath/src/github.com/unzipper'

alias riak_024='ssh -i ~/.ssh/skeleton01.pem ubuntu@ec2-54-147-151-152.compute-1.amazonaws.com'
pweb="ubuntu@ec2-54-80-237-5.compute-1.amazonaws.com"
alias pweb="ssh -i ~/.ssh/skeleton01.pem $pweb"

alias emr='ssh -i ~/.ssh/skeleton01.pem hadoop@ec2-54-167-88-56.compute-1.amazonaws.com'

# GoLang
export GOPATH="$HOME/gopath"
export PATH="$GOPATH/bin:$PATH"
export PATH=$PATH:/usr/local/go/bin
alias nt='cd $GOPATH/src/github.com/cloudability/night-train'
alias brn='cd $GOPATH/src/github.com/cloudability/burnside'
alias fc='cd $GOPATH/src/github.com/jackdesert/freecinc'

# TaskWarrior
#alias t='task -private'
export TASKDDATA=~/taskddata

# Jazz
alias ireal='cd ~/r/ireal'

alias start_babel='babel --watch jsx/ --out-dir static/'

# EliteCare
alias el='cd ~/r/elitecarerails'
alias f='cd ~/elitecare/elitecarerails/app/views/family_members/feed'
alias ad='view ~/elitecare/company_notes/advocate.md'
alias ade='vi  ~/elitecare/company_notes/advocate.md'
alias q='view  ~/elitecare/company_notes/questions.md'
alias qe='vi  ~/elitecare/company_notes/questions.md'

biptech='biptech@tabor.elitecare.com'
biptech_local='biptech@10.10.4.20'
alias biptech="ssh -A $biptech"
alias biptech_local="ssh -A $biptech_local"
alias b="ssh -A $biptech"
alias bl="ssh -A $biptech_local"

# Biptech2 is used for when on same network
biptech2='biptech@10.10.4.20'
alias biptech2="ssh -A $biptech2"


# Public IP Address using dynamic dns
# https://71.59.147.69:6001
router='jackdesert.no-ip.biz'
alias myip='nslookup jackdesert.no-ip.biz'
alias router_address='echo "jackdesert.no-ip.biz:6001"'

eloise="pi@$router"
eloise_port=2201
alias eloise="ssh -A $eloise -p $eloise_port"

hank="pi@$router"
hank_port=2202
alias hank="ssh -A $hank -p $hank_port"


sassie='pi@192.168.1.68'
alias sassie="ssh -A $sassie"
alias catch_sassie="echo 'CATCHING SASSIE ON LOCAL NETWORK ONLY USING PORT 22'; ssh -R 3000:localhost:3000 $sassie"

alias scp_help='echo "scp -P \$eloise_port <file> \$eloise:."'

alias catch_eloise="echo 'CATCHING ELOISE'; ssh -p $eloise_port -R 3000:localhost:3000 pi@$router"
alias catch_eloise_udp="echo 'CATCHING ELOISE UDP'; ssh -p $eloise_port -R 50005:localhost:50005 pi@$router"

bipproto='ubuntu@bipproto.biptech.org'
alias bipproto="ssh -A $bipproto"



jdbip='jdbip@tabor.elitecare.com'
alias jdbip="ssh -A $jdbip"

alias sleepytime='sleep 1800 && amixer set Master 0%'
alias jen='cd ~/.secret/letters/jen_gates'
alias jenu='vi ~/.secret/user_guides/jennifer.md'
alias jacku='vi ~/.secret/user_guides/jack.md'
alias real='vi ~/.secret/user_guides/getting_real_notes.md'
alias kids='vi ~/.kids'

alias jrn='vi /home/jd/.secret/journal/013_reconnection_with_jennifer.txt'
alias hrp='vi /home/jd/.secret/herpes.txt'
alias jack='vi ~/.secret/user_guides/jack.md'

# Volume
alias plus='amixer set 'Master' 5%+'
alias minus='amixer set 'Master' 5%-'

# Downloads
alias d='cd ~/downloads'
